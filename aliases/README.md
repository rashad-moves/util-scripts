Source `.aliases.sh` files in your `.bashrc`, ex
```bash
echo 'source util-sripts/aliases/git.aliases.sh' >> ~/.bashrc
cat ~/.bashrc
```

related idea:
[ohmybash aliases](https://github.com/ohmybash/oh-my-bash/tree/master/aliases)
